import { Dimensions } from "react-native";

export const LEFT_SWIPER_SPACE = 100;
export const RIGHT_SWIPER_SPACE = 100;

export const WIDTH = Dimensions.get("screen").width;
export const HEIGHT = Dimensions.get("screen").height;
